#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);   
    cin.tie(0);

    pair<int,int>p;
    p=make_pair(10,20);
    cout<<p.first<<" "<<p.second<<endl;

    return 0;
}